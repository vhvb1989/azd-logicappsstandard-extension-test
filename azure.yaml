# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: logicappsstandard-extension-test
metadata:
  template: vhvb1989/azd-logicappsstandard-extension-test
infra:
  provider: bicep
services:
  workflows:
    project: ./src/workflows
    host: function
    language: logicappsstandard
